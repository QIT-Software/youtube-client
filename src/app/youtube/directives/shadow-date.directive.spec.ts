import { ShadowDateDirective } from './shadow-date.directive';

describe('ShadowDateDirective', () => {
  it('should create an instance', () => {
    const directive: ShadowDateDirective = new ShadowDateDirective();
    expect(directive).toBeTruthy();
  });
});
