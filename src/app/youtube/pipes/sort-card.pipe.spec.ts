import { SortCardPipe } from './sort-card.pipe';

describe('SortCardPipe', () => {
  it('create an instance', () => {
    const pipe: SortCardPipe = new SortCardPipe();
    expect(pipe).toBeTruthy();
  });
});
