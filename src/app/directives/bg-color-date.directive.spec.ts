import { BgColorDateDirective } from './bg-color-date.directive';

describe('BgColorDateDirective', () => {
  it('should create an instance', () => {
    const directive: BgColorDateDirective = new BgColorDateDirective();
    expect(directive).toBeTruthy();
  });
});
